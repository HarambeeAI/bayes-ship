---
description: Autonomous build mode. Runs the entire build pipeline end-to-end.
---

Invoke the `ship-auto` skill to run the autonomous build pipeline. Pass any arguments (e.g. `--checkpoints`): $ARGUMENTS
